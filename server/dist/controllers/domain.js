'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
var fetchDomains = exports.fetchDomains = function fetchDomains(req, res, next) {
  console.log('herro');
  // console.log(req.user._id);
  // Domains.find({}, (err, result) => {
  //  if (err) { return next(err); }
  //  res.json(result);
  // });
};