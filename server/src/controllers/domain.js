export const fetchDomains = (req, res, next) => {
  console.log('herro');
  // console.log(req.user._id);
  // Domains.find({}, (err, result) => {
  //  if (err) { return next(err); }
  //  res.json(result);
  // });
};
